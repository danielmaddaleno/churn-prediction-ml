# Contributing

This is a personal project, but issues and pull requests are welcome.

Before opening a PR, please run `make test` and `make lint`, and keep the
style consistent (`black` / `isort`, line length 120).
